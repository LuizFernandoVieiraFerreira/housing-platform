{
  "name": "@housing-platform/backend-python",
  "private": true,
  "scripts": {
    "dev": "poetry run uvicorn housing_platform.main:app --reload --host 0.0.0.0 --port 8000",
    "build": "poetry build",
    "lint": "poetry run ruff check src tests scripts",
    "typecheck": "poetry run python -m compileall -q src",
    "test": "poetry run pytest"
  }
}
