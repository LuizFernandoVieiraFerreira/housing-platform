/**
 * Admin feature validation schemas.
 *
 * Add admin-specific schemas here as needed.
 * Re-export shared schemas from the validation package if applicable.
 */

// ============================================================================
// Feature-Local Schemas (if needed)
// ============================================================================

// Example: Add admin-specific schemas here
// export const adminFilterSchema = z.object({ ... });
